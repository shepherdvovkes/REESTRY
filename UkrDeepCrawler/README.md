# UkrDeepCrawler

Интеллектуальный deep crawler для поиска источников открытых данных в украинском сегменте интернета с использованием LMStudio.

## Возможности

- 🤖 **LLM-анализ контента** - автоматическое определение релевантных страниц и источников данных
- 🔍 **Умное извлечение ссылок** - фильтрация релевантных ссылок на реестры, API и файлы данных
- 📊 **Приоритизация обхода** - фокус на важных источниках данных
- 🎯 **Адаптивность** - работает с различными форматами без изменения кода
- 🇺🇦 **Специализация на украинских госпорталах** - оптимизирован для .gov.ua доменов

## Требования

- Python 3.8+
- LMStudio server (локально или удалённо)
- Playwright для JS-рендеринга

## Установка

```bash
pip install -r requirements.txt
playwright install chromium
```

## Использование

```python
from crawler import LLMCrawler

crawler = LLMCrawler(
    lmstudio_url="http://192.168.0.60:1234/v1/chat/completions"
)

seed_urls = [
    "https://data.gov.ua",
    "https://usr.minjust.gov.ua",
    "https://opendatabot.ua"
]

crawler.crawl(
    seed_urls=seed_urls,
    max_depth=4,
    max_pages=500
)

crawler.save_results("results.json")
```

## Конфигурация

Настройте LMStudio URL в `config.py` или передайте при инициализации:

```python
crawler = LLMCrawler(lmstudio_url="http://your-lmstudio-server:1234/v1/chat/completions")
```

## Структура проекта

```
UkrDeepCrawler/
├── crawler.py          # Основной класс LLMCrawler
├── llm_client.py       # Клиент для работы с LMStudio API
├── config.py          # Конфигурация
├── requirements.txt   # Зависимости
└── README.md          # Документация
```

## Результаты

Crawler сохраняет:
- Список релевантных URL с метаданными
- Статистику обхода
- Анализ типов источников (API, реестры, файлы данных)


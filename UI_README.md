# UI для управления датасетами и промптами LMStudio

Веб-интерфейс для управления датасетами, промптами и отслеживания истории обработки данных с помощью LLM через LMStudio API.

## Возможности

### 🎯 Управление промптами
- Создание, редактирование и удаление промптов
- Настройка системных и пользовательских промптов
- Привязка промптов к конкретным шагам алгоритма
- Настройка параметров генерации (temperature, max_tokens)
- Активация/деактивация промптов

### 📊 Управление датасетами
- Просмотр всех датасетов
- Создание новых датасетов
- Отслеживание статистики обработки
- Просмотр версий датасетов

### 📜 История обработки LLM
- Просмотр всех вызовов LLM с детальной информацией
- Фильтрация по шагам алгоритма и промптам
- Просмотр входных и выходных данных
- Анализ ошибок и метаданных
- Статистика по времени ответа и использованию токенов

### 🔍 Визуализация алгоритма
- Отображение всех точек обработки LLM в алгоритме
- Статистика по каждому шагу
- Список промптов, используемых на каждом шаге
- Количество вызовов на каждом этапе

## Быстрый старт

### Вариант 1: Автоматический запуск

```bash
./start_ui.sh
```

### Вариант 2: Ручной запуск

1. **Запуск backend:**
```bash
cd ui_backend
pip install -r requirements.txt
python app.py
```

Backend будет доступен на `http://localhost:5000`

2. **Запуск frontend:**
```bash
cd ui_frontend
python3 -m http.server 8000
```

Frontend будет доступен на `http://localhost:8000`

## Структура проекта

```
REESTRY/
├── ui_backend/           # Backend API (Flask)
│   ├── app.py           # Основной файл API
│   ├── requirements.txt # Зависимости
│   └── README.md        # Документация backend
│
├── ui_frontend/         # Frontend (HTML/CSS/JS)
│   ├── index.html      # Главная страница
│   ├── styles.css      # Стили
│   ├── app.js          # JavaScript логика
│   └── README.md       # Документация frontend
│
├── UkrDeepCrawler/
│   ├── llm_client.py   # Клиент LLM (с логированием)
│   ├── llm_logger.py   # Модуль логирования
│   └── crawler.py      # Краулер (интегрирован с логированием)
│
└── llm_history.db      # База данных истории (создается автоматически)
```

## Интеграция с краулером

Краулер автоматически логирует все вызовы LLM в базу данных. Для этого:

1. `LLMClient` был модифицирован для автоматического логирования
2. При каждом вызове `llm.call()` записывается:
   - Шаг алгоритма
   - Входные данные (промпты)
   - Выходные данные (ответ LLM)
   - Параметры (temperature, model)
   - Время ответа
   - Количество токенов
   - Статус (успех/ошибка)

## Шаги алгоритма

Система отслеживает следующие шаги алгоритма:

- `llm_analyze_page` - Анализ страницы на релевантность
- `llm_extract_relevant_links` - Извлечение релевантных ссылок
- `llm_parse_document` - Парсинг документа
- `llm_classify_content` - Классификация контента
- `llm_extract_metadata` - Извлечение метаданных

## API Endpoints

### Промпты
- `GET /api/prompts` - Список всех промптов
- `GET /api/prompts/<id>` - Получить промпт
- `POST /api/prompts` - Создать промпт
- `PUT /api/prompts/<id>` - Обновить промпт
- `DELETE /api/prompts/<id>` - Удалить промпт

### История
- `GET /api/llm-history` - История вызовов (фильтры: `limit`, `algorithm_step`, `prompt_id`)
- `GET /api/llm-history/<id>` - Детали вызова
- `POST /api/llm-history` - Записать вызов

### Датасеты
- `GET /api/datasets` - Список датасетов
- `GET /api/datasets/<id>` - Получить датасет
- `POST /api/datasets` - Создать датасет

### Статистика
- `GET /api/stats` - Общая статистика
- `GET /api/algorithm-steps` - Шаги алгоритма с LLM

## Использование

1. **Создайте промпты** для каждого шага алгоритма, где используется LLM
2. **Запустите краулер** - все вызовы LLM будут автоматически логироваться
3. **Просматривайте историю** в UI для отладки промптов
4. **Анализируйте результаты** - смотрите входные/выходные данные для каждого вызова
5. **Оптимизируйте промпты** - редактируйте промпты на основе результатов

## Примеры использования

### Создание промпта для анализа страницы

1. Откройте UI
2. Перейдите на вкладку "Промпты"
3. Нажмите "Создать промпт"
4. Заполните:
   - Название: "Анализ страницы на релевантность"
   - Шаг алгоритма: "llm_analyze_page"
   - Системный промпт: "Ты эксперт по анализу украинских государственных порталов..."
   - Шаблон пользовательского промпта: "Проанализируй эту страницу: {url}\n{content}"

### Просмотр истории обработки

1. Перейдите на вкладку "История обработки"
2. Используйте фильтры для поиска нужных вызовов
3. Кликните на запись для просмотра деталей
4. Анализируйте входные/выходные данные для отладки

### Визуализация алгоритма

1. Перейдите на вкладку "Алгоритм"
2. Просмотрите все шаги, где используется LLM
3. Увидьте статистику по каждому шагу
4. Проверьте, какие промпты используются на каждом этапе

## Технические детали

- **Backend**: Flask + SQLite
- **Frontend**: Vanilla JavaScript (без фреймворков)
- **База данных**: SQLite (автоматически создается)
- **CORS**: Включен для разработки

## Требования

- Python 3.7+
- Flask 3.0+
- flask-cors 4.0+

## Примечания

- База данных `llm_history.db` создается автоматически при первом запуске
- Все вызовы LLM из краулера автоматически логируются
- UI работает только в режиме разработки (CORS включен)
- Для продакшена рекомендуется настроить CORS правильно


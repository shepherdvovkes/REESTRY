# Быстрый старт UI для управления датасетами и промптами

## 🚀 Запуск за 3 шага

### 1. Установите зависимости backend

```bash
cd ui_backend
pip install -r requirements.txt
cd ..
```

### 2. Запустите backend

```bash
cd ui_backend
python app.py
```

Backend запустится на `http://localhost:5000`

### 3. Откройте frontend

В новом терминале:

```bash
cd ui_frontend
python3 -m http.server 8000
```

Или просто откройте `ui_frontend/index.html` в браузере.

## 📋 Что дальше?

1. **Создайте промпты** - перейдите на вкладку "Промпты" и создайте промпты для разных шагов алгоритма
2. **Запустите краулер** - все вызовы LLM будут автоматически логироваться
3. **Просматривайте историю** - на вкладке "История обработки" вы увидите все вызовы LLM
4. **Отлаживайте промпты** - просматривайте входные/выходные данные для каждого вызова

## 🎯 Основные функции

- **Промпты**: Создание и управление промптами для LLM
- **Датасеты**: Управление датасетами для обучения
- **История**: Просмотр всех вызовов LLM с деталями
- **Алгоритм**: Визуализация точек обработки LLM в алгоритме

## 🔧 Автоматический запуск

Используйте скрипт для автоматического запуска:

```bash
./start_ui.sh
```

Это запустит и backend, и frontend одновременно.

## 📖 Подробная документация

См. `UI_README.md` для полной документации.


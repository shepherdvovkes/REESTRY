# UI с Realtime обновлениями через WebSocket

Веб-интерфейс с поддержкой realtime обновлений через WebSocket для управления датасетами, промптами и отслеживания истории обработки LLM.

## ✨ Особенности

### Realtime обновления
- **Автоматическое обновление статистики** - статистика обновляется каждые 5 секунд
- **Мгновенное добавление новых вызовов LLM** - новые вызовы появляются в истории без перезагрузки
- **Обновление промптов в реальном времени** - изменения промптов видны всем подключенным клиентам
- **Индикатор подключения** - визуальный индикатор статуса WebSocket соединения

### Минималистичный дизайн
- Чистый и профессиональный интерфейс
- Современная цветовая палитра
- Плавные анимации и переходы
- Адаптивный дизайн для всех устройств

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd ui_backend
pip install -r requirements.txt
```

### 2. Запуск backend

```bash
cd ui_backend
python app.py
```

Backend запустится на `http://localhost:5000` с поддержкой WebSocket.

### 3. Открытие frontend

Откройте `ui_frontend/index.html` в браузере или запустите локальный сервер:

```bash
cd ui_frontend
python3 -m http.server 8000
```

Затем откройте `http://localhost:8000`

## 🔌 WebSocket события

### Клиент → Сервер

- `connect` - Подключение клиента
- `subscribe_stats` - Подписка на обновления статистики
- `subscribe_history` - Подписка на обновления истории

### Сервер → Клиент

- `connected` - Подтверждение подключения
- `stats_update` - Обновление статистики (каждые 5 секунд)
- `stats_updated` - Уведомление об изменении статистики
- `llm_call_logged` - Новый вызов LLM добавлен в историю
- `prompt_created` - Создан новый промпт
- `prompt_updated` - Промпт обновлен
- `prompt_deleted` - Промпт удален

## 📊 Realtime функции

### Статистика
- Обновляется автоматически каждые 5 секунд
- Обновляется при создании/удалении промптов
- Обновляется при новых вызовах LLM

### История обработки
- Новые вызовы LLM появляются мгновенно в начале списка
- Анимация появления новых элементов
- Автоматическое соблюдение фильтров
- Ограничение количества элементов по лимиту

### Промпты
- Изменения промптов видны всем подключенным клиентам
- Автоматическое обновление списка при изменениях

## 🎨 Дизайн

### Цветовая палитра
- Primary: `#2563eb` (синий)
- Success: `#10b981` (зеленый)
- Error: `#ef4444` (красный)
- Background: `#ffffff` (белый)
- Secondary Background: `#f8fafc` (светло-серый)

### Типографика
- Шрифт: System fonts (Inter, Segoe UI, Roboto)
- Размеры: 12px, 14px, 16px, 20px, 24px
- Веса: 400 (regular), 500 (medium), 600 (semibold)

### Компоненты
- Скругления: 4px (малые), 8px (средние)
- Тени: Мягкие, многослойные
- Переходы: 0.2s ease для всех интерактивных элементов

## 🔧 Технические детали

### Backend
- Flask-SocketIO для WebSocket поддержки
- Eventlet для асинхронной обработки
- Фоновый поток для периодической статистики
- Автоматическая отправка событий при изменениях

### Frontend
- Socket.IO клиент для WebSocket
- Автоматическое переподключение при разрыве связи
- Оптимизация обновлений (избежание дубликатов)
- Плавные анимации для новых элементов

## 📝 Использование

1. **Откройте UI** в браузере
2. **Проверьте индикатор подключения** в правом нижнем углу
3. **Создайте промпты** - изменения видны в реальном времени
4. **Запустите краулер** - новые вызовы LLM появляются автоматически
5. **Наблюдайте за статистикой** - обновляется каждые 5 секунд

## 🐛 Отладка

### Проверка WebSocket подключения
- Индикатор в правом нижнем углу должен быть зеленым
- В консоли браузера должны быть сообщения о подключении
- Проверьте, что backend запущен и доступен

### Проблемы с подключением
- Убедитесь, что backend запущен на порту 5000
- Проверьте CORS настройки
- Проверьте консоль браузера на ошибки

## 📚 Дополнительная документация

- `UI_README.md` - Полная документация UI
- `QUICKSTART_UI.md` - Быстрый старт
- `ui_backend/README.md` - Документация API

